name: Validate Kustomize Configurations

on:
  push:

jobs:
  validate-kustomize:
    # TEMP: tracking the actions feature branch until datum-cloud/actions#80
    # (build with LoadRestrictionsNone) merges and a tag is cut, then pin back.
    uses: datum-cloud/actions/.github/workflows/validate-kustomize.yaml@fix/kustomize-load-restrictor
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: connectors.networking.datumapis.com
spec:
  group: networking.datumapis.com
  names:
    kind: Connector
    listKind: ConnectorList
    plural: connectors
    singular: connector
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Connector is the Schema for the connectors API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of a Connector
            properties:
              capabilities:
                description: |-
                  Capabilities desired to be supported by the connector.

                  A connector may choose to not support all requested capabilities, and may
                  also choose to support additional capabilities not requested here. The
                  condition of each capability will reflect whether the capability is supported
                  or not in the ConnectorStatus.
                items:
                  properties:
                    connectTCP:
                      properties:
                        disabled:
                          type: boolean
                      type: object
                    type:
                      description: Type of capability
                      maxLength: 253
                      minLength: 1
                      type: string
                  required:
                  - type
                  type: object
                maxItems: 16
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              connectorClassName:
                minLength: 1
                type: string
            required:
            - connectorClassName
            type: object
          status:
            default:
              conditions:
              - lastTransitionTime: "1970-01-01T00:00:00Z"
                message: Waiting for controller
                reason: Pending
                status: Unknown
                type: Accepted
            description: Status defines the observed state of a Connector
            properties:
              capabilities:
                description: Capabilities describe the status of each capability of
                  the connector.
                items:
                  properties:
                    conditions:
                      description: Conditions describe the current conditions of the
                        capability.
                      items:
                        description: Condition contains details for one aspect of
                          the current state of this API Resource.
                        properties:
                          lastTransitionTime:
                            description: |-
                              lastTransitionTime is the last time the condition transitioned from one status to another.
                              This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                            format: date-time
                            type: string
                          message:
                            description: |-
                              message is a human readable message indicating details about the transition.
                              This may be an empty string.
                            maxLength: 32768
                            type: string
                          observedGeneration:
                            description: |-
                              observedGeneration represents the .metadata.generation that the condition was set based upon.
                              For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                              with respect to the current state of the instance.
                            format: int64
                            minimum: 0
                            type: integer
                          reason:
                            description: |-
                              reason contains a programmatic identifier indicating the reason for the condition's last transition.
                              Producers of specific condition types may define expected values and meanings for this field,
                              and whether the values are considered a guaranteed API.
                              The value should be a CamelCase string.
                              This field may not be empty.
                            maxLength: 1024
                            minLength: 1
                            pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                            type: string
                          status:
                            description: status of the condition, one of True, False,
                              Unknown.
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            description: type of condition in CamelCase or in foo.example.com/CamelCase.
                            maxLength: 316
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                            type: string
                        required:
                        - lastTransitionTime
                        - message
                        - reason
                        - status
                        - type
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - type
                      x-kubernetes-list-type: map
                    type:
                      description: Type of capability
                      maxLength: 253
                      minLength: 1
                      type: string
                  required:
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              conditions:
                description: Conditions describe the current conditions of the HTTPProxy.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              connectionDetails:
                description: ConnectionDetails provide details on how to connect to
                  the connector.
                properties:
                  publicKey:
                    description: PublicKey connection details
                    properties:
                      addresses:
                        description: Addresses where the connector can be reached
                        items:
                          description: PublicKeyConnectorAddress defines an address
                            and port for a connector.
                          properties:
                            address:
                              description: IPv4 or IPv6 address.
                              maxLength: 39
                              type: string
                              x-kubernetes-validations:
                              - message: Must be an IP address.
                                rule: isIP(self)
                            port:
                              description: Port where the connector can be reached.
                              format: int32
                              maximum: 65535
                              minimum: 1
                              type: integer
                          required:
                          - address
                          - port
                          type: object
                        maxItems: 16
                        type: array
                      discoveryMode:
                        default: DNS
                        description: The mode used to discover the public key
                        enum:
                        - DNS
                        type: string
                      homeRelay:
                        description: |-
                          Home Relay server of the connector

                          Must be a valid URL
                        type: string
                        x-kubernetes-validations:
                        - message: Must be a URL.
                          rule: isURL(self)
                      id:
                        description: The public key to dial and connect to
                        type: string
                    required:
                    - addresses
                    - homeRelay
                    type: object
                  type:
                    description: Type of connection details provided.
                    enum:
                    - PublicKey
                    type: string
                required:
                - type
                type: object
                x-kubernetes-validations:
                - message: publicKey field must be nil if the type is not PublicKey
                  rule: '!(self.type != ''PublicKey'' && has(self.publicKey))'
                - message: publicKey field must be specified if the type is PublicKey
                  rule: self.type == 'PublicKey' && has(self.publicKey)
              leaseRef:
                description: |-
                  LeaseRef references the Lease used to report connector liveness.

                  The connector controller creates the Lease when a Connector is created
                  and records it here. Connector implementations (agents) are expected to
                  periodically renew the Lease to indicate liveness.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            type: object
        required:
        - spec
        type: object
    selectableFields:
    - jsonPath: .status.connectionDetails.publicKey.id
    served: true
    storage: true
    subresources:
      status: {}

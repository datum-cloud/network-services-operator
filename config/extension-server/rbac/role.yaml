---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: network-services-operator
    app.kubernetes.io/component: envoy-gateway-extension-server
    app.kubernetes.io/managed-by: kustomize
  name: envoy-gateway-extension-server-role
rules:
  - apiGroups:
      - ""
    resources:
      - namespaces
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - networking.datumapis.com
    resources:
      - trafficprotectionpolicies
      - httpproxies
      - connectors
    verbs:
      - get
      - list
      - watch
  # Reads the CNI-published EndpointSlice a vpcPod HTTPProxy backend
  # references, to resolve the tenant-id label the socket-bind mutation
  # keys on (#856). This file is hand-maintained — `make manifests` does
  # not generate it.
  - apiGroups:
      - discovery.k8s.io
    resources:
      - endpointslices
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - networking.datumapis.com
    resources:
      - httpproxies/status
      - connectors/status
    verbs:
      - get
  # Edge-local Programmed feedback for TrafficProtectionPolicy (NSO#266). The
  # extension server writes status only on the edge replica after a successful
  # WAF apply; upstream NSO mirrors the Karmada-aggregated value to tenants.
  - apiGroups:
      - networking.datumapis.com
    resources:
      - trafficprotectionpolicies/status
    verbs:
      - get
      - update
      - patch
  # The edge re-translation controller patches a trigger annotation onto
  # Gateways to make Envoy Gateway re-translate when a Connector's liveness
  # changes.
  - apiGroups:
      - gateway.networking.k8s.io
    resources:
      - gateways
    verbs:
      - get
      - patch

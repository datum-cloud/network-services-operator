apiVersion: quota.miloapis.com/v1alpha1
kind: ClaimCreationPolicy
metadata:
  name: project-httproutefilter-claim-policy
  labels:
    app.kubernetes.io/name: network-services-operator
    app.kubernetes.io/component: quota-system
spec:
  trigger:
    resource:
      apiVersion: gateway.envoyproxy.io/v1alpha1
      kind: HTTPRouteFilter
  target:
    resourceClaimTemplate:
      metadata:
        name: "httproutefilter-{{ trigger.metadata.name }}"
        namespace: "{{requestInfo.namespace}}"
        labels:
          app.kubernetes.io/name: network-services-operator
          app.kubernetes.io/component: quota-system
        annotations:
          kubernetes.io/description: "Automatic quota claim for HTTPRouteFilter creation"
      spec:
        requests:
          - resourceType: gateway.envoyproxy.io/httproutefilters
            amount: 1

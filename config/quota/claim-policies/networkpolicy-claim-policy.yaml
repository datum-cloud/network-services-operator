apiVersion: quota.miloapis.com/v1alpha1
kind: ClaimCreationPolicy
metadata:
  name: project-networkpolicy-claim-policy
  labels:
    app.kubernetes.io/name: network-services-operator
    app.kubernetes.io/component: quota-system
spec:
  trigger:
    resource:
      apiVersion: networking.datumapis.com/v1alpha
      kind: NetworkPolicy
  target:
    resourceClaimTemplate:
      metadata:
        name: "networkpolicy-{{ trigger.metadata.name }}"
        namespace: "{{requestInfo.namespace}}"
        labels:
          app.kubernetes.io/name: network-services-operator
          app.kubernetes.io/component: quota-system
        annotations:
          kubernetes.io/description: "Automatic quota claim for NetworkPolicy creation"
      spec:
        requests:
          - resourceType: networking.datumapis.com/networkpolicies
            amount: 1

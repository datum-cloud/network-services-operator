apiVersion: networking.datumapis.com/v1alpha
kind: SubnetClaim
metadata:
  labels:
    app.kubernetes.io/name: network-services-operator
    app.kubernetes.io/managed-by: kustomize
  name: subnetclaim-sample
spec:
  # TODO(user): Add fields here

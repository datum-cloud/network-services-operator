resources:
  - namespace.yaml
helmCharts:
  - name: gateway-helm
    includeCRDs: true
    namespace: envoy-gateway-system
    releaseName: envoy-gateway
    version: v1.3.2
    repo: oci://docker.io/envoyproxy

# Edge Gateway UI Environment Variables

# Set to 'true' to use mock data instead of real Kubernetes API
# Useful for local development without a cluster
NEXT_PUBLIC_USE_MOCK_DATA=false

# Path to kubeconfig file for local development
# If not set, falls back to in-cluster config or ~/.kube/config
# Example: KUBECONFIG=../.test-infra/kubeconfig
# KUBECONFIG=

# Node environment (automatically set by Next.js)
# NODE_ENV=development
